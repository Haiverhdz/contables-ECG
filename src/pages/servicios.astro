---
import Layout from '../layouts/Layout.astro';
import ServiceCard from '../components/ServiceCard.astro';
import CtaBand from '../components/CtaBand.astro';
import { services } from '../data/services';
---

<Layout title="Servicios - Contables ECG">
  <section class="hero-glow bg-gradient-brand">
    <div class="max-w-6xl mx-auto px-4 py-16 md:py-20">
      <p class="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-white mb-4">
        <span class="w-1.5 h-1.5 rounded-full bg-white/80"></span>
        Servicios
      </p>
      <h1 class="text-4xl md:text-5xl leading-[1.12] text-white mb-4 max-w-2xl">
        Soluciones contables y tributarias, de punta a <em class="italic text-white/80">punta.</em>
      </h1>
      <p class="text-lg text-white/85 max-w-2xl leading-relaxed">
        Ofrecemos acompañamiento integral en materia contable, tributaria y laboral para empresas y
        personas naturales.
      </p>
    </div>
  </section>

  <section class="bg-surface-sunken/60">
    <div class="max-w-6xl mx-auto px-4 py-16 md:py-20">
      <div class="grid md:grid-cols-2 gap-6">
        {services.map((service) => (
          <ServiceCard icon={service.icon} title={service.title} items={service.items} />
        ))}
      </div>
    </div>
  </section>

  <section class="bg-gradient-brand">
    <div class="max-w-6xl mx-auto px-4 py-16 md:py-20 text-center">
      <p class="text-xs font-semibold uppercase tracking-wide text-white mb-3">Nuestro equipo</p>
      <h2 class="text-3xl md:text-4xl text-white mb-4">Conozca a quienes hacen posible cada servicio</h2>
      <p class="text-white/80 max-w-xl mx-auto mb-10 leading-relaxed">
        Un equipo cercano, atento a los detalles y comprometido con el cumplimiento de sus
        obligaciones.
      </p>
      <img
        src="/images/quienes-somos.webp"
        alt="Equipo de trabajo Contables ECG"
        class="rounded-2xl shadow-card mx-auto max-h-[420px] w-full max-w-3xl object-cover"
      />
    </div>
  </section>

  <CtaBand />
</Layout>
